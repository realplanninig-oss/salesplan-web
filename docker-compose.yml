services:
  web:
    build: .
    container_name: salesplan-web
    ports:
      - "8000:8000"
    environment:
      - ADMIN_USERNAME=admin
      - ADMIN_PASSWORD=admin123
      - PORT=8000
    restart: unless-stopped
